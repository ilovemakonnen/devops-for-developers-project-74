
version: "3.9"

services:
  app:
    image: ilovemakonnen/devops-for-developers-project-74
    ports:
      - "8081:8080"
    environment:
      NODE_ENV: production
      DATABASE_URL: postgres://postgres:password@db:5432/blog